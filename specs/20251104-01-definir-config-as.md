Tarefa: Definir parâmetros no config.py para o assignment da AS

Descrição:

Na spec `20251103-01-criar-prompt-as` criamos o prompt para o novo assignment `prog2-as`. Mas, se olharmos as instruções desse assignment (`README.md`, `PROVA_PARTE1.md`, `PROVA_PARTE2.md` e `PROVA_PARTE3.md`) veremos que ele inclui uma aplicação de terminal e uma de streamlit. Essas aplicações exigem uma parametrização adequada no `config.py`. Faça, portanto, o ajuste necessário nesse arquivo. 